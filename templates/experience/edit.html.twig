{% import '/macro/section.html.twig' as section %}
{% extends 'base.html.twig' %}

{% block title %}Edit Experience{% endblock %}

{% block body %}
{{section.sectionstartDefault("Edit Experience")}}

    {{ include('experience/_form.html.twig', {'button_label': 'Update', 'button_class':'btn-secondary'}) }}

    <a href="{{ path('app_experience_index') }}" class="btn btn-outline-primary">Back to list</a>

    {{section.sectionend()}}
{% endblock %}
