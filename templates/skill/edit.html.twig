{% import '/macro/section.html.twig' as section %}
{% extends 'base.html.twig' %}

{% block title %}Edit Skill{% endblock %}

{% block body %}
{{section.sectionstartDefault("Edit Skill")}}

    {{ include('skill/_form.html.twig', {'button_label': 'Update', 'button_class':'btn-secondary'}) }}

    <a href="{{ path('app_skill_index') }}" class="btn btn-outline-primary">back to list</a>

    {{ include('skill/_delete_form.html.twig') }}
    {{section.sectionend()}}
{% endblock %}
